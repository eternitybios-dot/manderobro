{
  "name": "manderobro",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "verify:motion": "node scripts/verify-motion.mjs",
    "verify:seamless": "node scripts/verify-seamless.mjs",
    "deploy": "npm run build && npx -y @shipstatic/ship ./dist --json"
  },
  "devDependencies": {
    "pixelmatch": "^7.2.0",
    "pngjs": "^7.0.0",
    "puppeteer-core": "^25.3.0",
    "vite": "^6.0.7"
  }
}
