{
  "name": "manderobro",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "verify:motion": "node scripts/verify-motion.mjs",
    "deploy": "npm run build && npx -y @shipstatic/ship ./dist --json"
  },
  "devDependencies": {
    "vite": "^6.0.7"
  }
}
